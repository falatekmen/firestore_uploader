{
  "name": "upload_json_to_firestore",
  "version": "1.0.0",
  "description": "Upload a JSON file to Firebase Firestore",
  "main": "uploader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "falat",
  "license": "MIT",
  "dependencies": {
    "firebase": "^9.6.11",
    "firebase-admin": "^10.1.0"
  }
}
